# Compiler Templates

This directory is reserved for marketplace compiler templates.

Current implementation renders templates from Python render functions in
`app/compiler/render.py` to keep generation deterministic and easy to test.
